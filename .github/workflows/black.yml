name: Black

on:
  push:
    branches:
      - main
      - /^release-.*$/
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  black:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: psf/black@stable